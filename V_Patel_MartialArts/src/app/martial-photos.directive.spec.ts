import { MartialPhotosDirective } from './martial-photos.directive';

describe('MartialPhotosDirective', () => {
  it('should create an instance', () => {
    const directive = new MartialPhotosDirective();
    expect(directive).toBeTruthy();
  });
});
